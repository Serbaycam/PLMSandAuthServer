namespace PLMS.Service.Validations
{
    public class ProductDtoValidator//:AbstractValidator<ProductDto>
    {
        //public ProductDtoValidator()
        //{
        //    RuleFor(x => x.Name).NotNull().WithMessage("{PropertyName} is required").NotEmpty().WithMessage("{PropertyName} is required");
        //    RuleFor(x => x.Price).GreaterThan(1).WithMessage("{PropertyName} is must be greater than 0");
        //    RuleFor(x=>x.CategoryId).NotNull().WithMessage("{PropertyName} is required").NotEmpty().WithMessage("{PropertyName} is required");
        //}
    }
}
